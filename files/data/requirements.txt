# files/data/requirements.txt
# Python development environment for the thesis ML codebase.
# Installed by Playbook 10 into /root/thesis-infra/.venv

# --- Data versioning ---
# Pinned to a version that supports current pathspec >= 0.12.1
# (fixes "cannot import name '_DIR_MARK'" issue in older dvc).
dvc[s3]>=3.59,<4.0

# --- ML core ---
numpy>=1.26,<2.0
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0

# --- Gradient boosting (Notebook 05 model comparison) ---
xgboost>=2.0,<3.0

# --- Deep learning ---
torch>=2.3,<3.0
pytorch-lightning>=2.3,<3.0
torchmetrics>=1.4,<2.0

# --- Experiment tracking + hyperparameter search ---
mlflow>=2.18,<3.0
optuna>=4.0,<5.0
boto3>=1.34,<2.0

# --- Drift detection ---
evidently>=0.4,<0.5

# --- Notebooks + visualization ---
jupyterlab>=4.2,<5.0
ipykernel>=6.29,<7.0
matplotlib>=3.9,<4.0
seaborn>=0.13,<0.14
plotly>=5.22,<6.0

# --- HTTP client (Notebook 04 drift simulation, healthcheck calls) ---
requests>=2.32,<3.0

# --- Utilities ---
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
tqdm>=4.66,<5.0

# Testing
pytest>=8.0,<9.0
