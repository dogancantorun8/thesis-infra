# KFP pipeline component runtime dependencies
# Used by: load_data_op, train_lstm_op, register_model_op, trigger_rollout_op
#
# Versions PINNED to match v56 notebook baseline conda.yaml exactly
# (s3://thesis-mlflow/9/9f608316.../artifacts/model/conda.yaml)
#
# Rationale (EC#32): PyTorch 2.5 + NumPy 2.x exhibited LSTM training plateau
# (mean-prediction collapse) for stacked 2-layer LSTMs at lr=0.001. Pinning to
# v56's exact environment ensures reproducibility with notebook baseline.

mlflow==2.22.5
numpy==1.26.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.5.2
boto3==1.35.0

# PyTorch CPU-only — pinned to v56's training version (2.12.0)
# Combined with forget gate bias=1 init in src/model.py, this reproduces
# the notebook baseline RMSE (~13.5) in script execution context.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu

# Kubernetes client
kubernetes==30.1.0

# DVC + S3 backend — reproducibility argument (DVC content hash addressing)
dvc[s3]==3.67.1
